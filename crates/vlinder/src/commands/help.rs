/// Run the bundled support fleet.
///
/// Not yet implemented — the support fleet has been moved to a separate
/// repository and is not ready for bundling.
#[allow(clippy::unused_async)]
pub async fn execute() {
    eprintln!("Support fleet is not yet available.");
    eprintln!("See: https://github.com/vlindercli/sample-agents-fleets");
    std::process::exit(1);
}
