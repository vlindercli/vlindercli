[package]
name = "vlinder-amqp"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
vlinder-core = { path = "../vlinder-core" }
async-trait = "0.1"
lapin = "3.0"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time"] }
serde_json = "1"
futures = "0.3"
tracing = "0.1"

[lints]
workspace = true
